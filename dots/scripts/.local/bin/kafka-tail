#!/usr/bin/env ruby

require 'kafka'

kafka_servers = ENV.fetch('KAFKA_SERVERS', 'localhost:9092')

kafka = Kafka.new(
  seed_brokers: [kafka_servers],
)

topic = ARGV[0]

puts "Following topic #{topic}"

kafka.each_message(topic: topic) do |message|
  puts message.value
end
